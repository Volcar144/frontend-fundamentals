// Tailwind CSS v4 uses CSS-first config — this file is no longer needed.
// All configuration is in src/app/globals.css via @import directives.
// Kept as an empty placeholder to avoid any tooling that expects this file.
